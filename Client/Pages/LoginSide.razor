@page "/login"
@using MiljøFestivalv2.Shared
@using System.Net.Http.Json
@using Client.Services
@using System.Globalization
@inject NavigationManager NavManager
@inject HttpClient Http


<h3>Login</h3>

<div>
    <label>Email:</label>
    <input type="email" @bind="email" />
</div>
<div>
    <label>Password:</label>
    <input type="password" @bind="password" />
</div>
<button @onclick="Login">Login</button>
<button @onclick="Register">Register</button>

@code {
    private string email;
    private string password;

    private void Login()
    {
        // Implementer login-logik her
        // Eksempelvis kan du tjekke email og password mod en database eller anden form for autentificering
        // Hvis login er succesfuldt, kan du navigere brugeren til en anden side  (ref til 2 forskellige sider: frivillig og koordinator med if statement)
        // NavManager.NavigateTo("/dashboard");
    }

    private void Register()
    {
        NavManager.NavigateTo("/register");
    }
}
